"""`reportes` no consulta ninguna tabla ni vista directamente: toda su
lectura pasa por el `service` de `ventas`/`inventario`/`reservas`/
`probador` (P6.3). Este archivo existe por consistencia con la estructura
de paquete de `CLAUDE.md`, pero no define ningún acceso a datos propio."""
