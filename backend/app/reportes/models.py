"""`reportes` no tiene tablas propias: solo lee, combinando datos de
`ventas`/`inventario`/`reservas`/`probador` a través del `service` de cada
uno (P6.3). Este archivo existe por consistencia con la estructura de
paquete de `CLAUDE.md`, pero no define ningún modelo."""
